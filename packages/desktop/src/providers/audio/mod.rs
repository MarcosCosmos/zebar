pub mod common;

#[cfg(any(
  target_os = "linux",
  target_os = "dragonfly",
  target_os = "freebsd",
  target_os = "netbsd",
  target_os = "openbsd"
))]
mod pulseaudio;

#[cfg(any(
  target_os = "linux",
  target_os = "dragonfly",
  target_os = "freebsd",
  target_os = "netbsd",
  target_os = "openbsd"
))]
pub use {common::*, pulseaudio::*};

#[cfg(windows)]
mod windows;

#[cfg(windows)]
pub use windows::*;
